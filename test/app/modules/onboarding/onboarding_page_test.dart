import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('OnboardingPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(OnboardingPage(title: 'Onboarding')));
    //  final titleFinder = find.text('Onboarding');
    //  expect(titleFinder, findsOneWidget);
  });
}
