import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:desktop/app/modules/note/note_module.dart';

void main() {
  initModule(NoteModule());
  // NoteController note;
  //
  setUp(() {
    //     note = NoteModule.to.get<NoteController>();
  });

  group('NoteController Test', () {
    //   test("First Test", () {
    //     expect(note, isInstanceOf<NoteController>());
    //   });

    //   test("Set Value", () {
    //     expect(note.value, equals(0));
    //     note.increment();
    //     expect(note.value, equals(1));
    //   });
  });
}
