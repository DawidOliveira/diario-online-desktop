import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('NotePage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(NotePage(title: 'Note')));
    //  final titleFinder = find.text('Note');
    //  expect(titleFinder, findsOneWidget);
  });
}
